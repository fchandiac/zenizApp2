/**
 * test with page objects
 */


describe('Layout', () => {
    before(() => {
        browser.url('http://localhost:3000/');
    })

    // it('Existe AppBar', async () => {
    //     const appBar = await $('[name="appBar"]')
    //     await expect(appBar).toExist()
   
    // })

    // it('Existe Botón Menu', async () => {
    //     const menuButton = await $('[name="menuButton"]')
    //     await expect(menuButton).toExist()
    // })

    it('Botón Menu despliega menu', async () => {
   
    })

    it('Elemento del Menú redirecciona', async () => {
   
    })
})

